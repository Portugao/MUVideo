{# Purpose of this template: Display collections within an external context #}
{% for collection in items %}
    <h3>{{ collection|muvideomodule_formattedTitle }}</h3>
    <p><a href="{{ path('muvideomodule_collection_display', {'id': collection.getId()}) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}

{# Purpose of this template: Display movies within an external context #}
{% for movie in items %}
    <h3>{{ movie.getTitleFromDisplayPattern() }}</h3>
    <p><a href="{{ path('muvideomodule_movie_display', { 'id': movie.id }) }}">{{ __('Read more') }}</a>
    </p>
{% endfor %}
